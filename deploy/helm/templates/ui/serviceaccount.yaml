{{- if and .Values.ui.enabled .Values.ui.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "gaussia-evalhub.uiServiceAccountName" . }}
  labels:
    {{- include "gaussia-evalhub.uiLabels" . | nindent 4 }}
{{- end }}
